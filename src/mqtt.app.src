{application, mqtt,
    [
        {description, "Mqtt server"},
        {vsn, "0.0.5"},
        {registered, []},
        {applications, [
            kernel,
            stdlib,
            mnesia,
            ranch,
            poolboy
        ]},
        {mod, { mqtt_app, []}},
        {env, []}
    ]}.
