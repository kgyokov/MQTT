{application, mqtt,
    [
        {description, "Mqtt server"},
        {vsn, "0.0.3"},
        {registered, []},
        {applications, [
            kernel,
            stdlib,
            mnesia,
%%                   crypto,
%%                   public_key,
%%                   sasl,
            ranch,
            poolboy
        ]},
        {mod, { mqtt_app, []}},
        {env, []}
    ]}.
